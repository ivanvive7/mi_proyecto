package mi_proyecto;

/**
 * Clase de demostración que imprime un mensaje de "Nota" por consola.
 *
 * <p>Esta clase forma parte del proyecto {@code mi_proyecto} y sirve como
 * ejemplo básico de un programa Java ejecutable. Contiene únicamente un
 * método {@code main} que muestra el texto "Nota" en la salida estándar.</p>
 *
 * <p><strong>Nota:</strong> El nombre del fichero ({@code nota.java}) no
 * coincide con el nombre de la clase ({@code Hola}). Según las convenciones
 * de Java, ambos deberían coincidir.</p>
 *
 * @author ivanvive7
 * @version 1.0
 */
public class Hola {

	/**
	 * Punto de entrada de la aplicación.
	 *
	 * <p>Imprime el mensaje {@code "Nota"} en la salida estándar
	 * ({@link System#out}).</p>
	 *
	 * @param args argumentos de línea de comandos (no se utilizan)
	 */
	public static void main(String[] args) {
		System.out.println("Nota");
	}

}
